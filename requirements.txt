# Panda MINI Soundboard — runtime dependencies (Python 3.9+)

# GUI
PySide6>=6.6

# Audio output and decoding
sounddevice>=0.4.6        # PortAudio (bundled in the Windows wheel)
soundfile>=0.12.1         # WAV/FLAC/OGG/MP3/AIFF (libsndfile 1.1+ has MP3)
numpy>=1.24
av>=12.0                  # FFmpeg: M4A, AAC, WMA, OPUS, MP4/MOV/MKV/WEBM video ...
soxr>=0.3                 # high-quality sample-rate conversion

# MIDI (Windows uses the built-in WinMM API, nothing extra needed)
mido>=1.3; sys_platform != "win32"
python-rtmidi>=1.5; sys_platform != "win32"
